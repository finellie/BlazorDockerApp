# Copy this file to .env and adjust the values before running docker compose.
#   cp .env.example .env

# PostgreSQL settings
POSTGRES_DB=blazordockerapp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me

# Host port for the Blazor web app
WEB_PORT=9115
